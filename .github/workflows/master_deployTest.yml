# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and Deploy

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@master

    - name: Set up .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '5.0.x'

    - name: dotnet publish
      run: dotnet publish -c Release -o ${{env.DOTNET_ROOT}}/myapp -r win-x86 

    - name: Zip output
      run: |
        cd ${{env.DOTNET_ROOT}}/myapp
        zip publish.zip *.*



    - name: Upload a Build Artifact
      uses: actions/upload-artifact@v2.2.1
      with:
          # Artifact name
          name: GaAspCore
          # A file, directory or wildcard pattern that describes what to upload
          path: ${{env.DOTNET_ROOT}}/myapp/publish.zip
          # The desired behavior if no files are found using the provided path.           


  deploy1:
    runs-on: ubuntu-latest
    needs: build
    steps:
    - name: Download a Build Artifact
      uses: actions/download-artifact@v2
      with:
          name: GaAspCore
          path: myapp

    #- name: Deploy to Azure Web App
    #  uses: azure/webapps-deploy@v2
    #  with:
    #    app-name: 'ykmcoin'
    #    slot-name: 'production'
    #    publish-profile: ${{ secrets.AzureAppService_PublishProfile_7014e788a62947829b2a718bf670e78b }}
    #    package: myapp 


    #- name: Azure WebApp
    #  uses: Azure/webapps-deploy@v2
    #  with:
    #    # Name of the Azure Web App
    #    app-name: GaAspCore
    #    # Applies to Web Apps(Windows and Linux) and Web App Containers(linux). Multi container scenario not supported. Publish profile (*.publishsettings) file contents with Web Deploy secrets
    #    publish-profile: ${{ secrets.GAASPCORE_PUBLISHPROFILE }}
    #    # Enter an existing Slot other than the Production slot
    #    package: ${{env.DOTNET_ROOT}}/myapp    

    - name: Azure WebApp
      uses: Azure/webapps-deploy@v2
      with:
        # Name of the Azure Web App
        app-name: GaAspCore
        # Applies to Web Apps(Windows and Linux) and Web App Containers(linux). Multi container scenario not supported. Publish profile (*.publishsettings) file contents with Web Deploy secrets
        publish-profile: ${{ secrets.GAASPCORE }}
        # Enter an existing Slot other than the Production slot
        package: myapp    
