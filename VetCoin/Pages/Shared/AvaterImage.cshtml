@model Data.VetMember
@if (Model != null)
{
    if (Model.DiscordId != 0)
    {
    <img src="https://cdn.discordapp.com/avatars/@(Model.DiscordId)/@(Model.AvatarId).png?size=128" height="32" width="32" />
    }
}

