@model Data.VetMember
@if (Model != null)
{
    if (Model.DiscordId != 0)
    {
        var url = Model.GetAvaterIconUrl();
        if (!string.IsNullOrEmpty(url))
        {
        <img src="@url" height="32" width="32" />
        }
    }
}

