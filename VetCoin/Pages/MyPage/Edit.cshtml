@page
@model EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>表示名変更</h1>

<h4>VetMember</h4>

<p>VET内での表示名と異なる方はこちらで変更してください</p>
<p>それ以外の用途での変更は原則行わないでください</p>

<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="VetMember.Id" />

            <div class="form-group">
                <label asp-for="VetMember.Name" class="control-label"></label>
                <input asp-for="VetMember.Name" class="form-control" />
                <span asp-validation-for="VetMember.Name" class="text-danger"></span>
            </div>

            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
